import React from 'react';
// import { shallow } from 'enzyme';
import App from './App';
// jest.mock("./components/Form1/style.scss", () => jest.fn());
describe('<App />', () => {

  test('should export App component', () => {
    // App.should.be.ok();
    
  });

  // it('should have a <Layout /> component',() => {
  //   const wrapper = shallow(<App />);
  //   wrapper.find('Layout').should.be.ok();
  // });

  // it('should have a <Route /> component',() => {
  //   const wrapper = shallow(<App />);
  //   wrapper.find('Route').should.be.ok();
  // });
});
